{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "(uv pip install --system -r requirements.txt || python3 -m pip install --break-system-packages -r requirements.txt) && cd frontend && npm ci",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
